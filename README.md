Sources for the plugin `plugin.applovinMax.Mintegral`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.Mintegral" = {
            publisherId = "com.solar2d",
        },
    },
}
```
